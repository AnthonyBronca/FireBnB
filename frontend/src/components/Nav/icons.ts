import { icon } from "@fortawesome/fontawesome-svg-core/import.macro";

// const search = icon({name: 'magnifying-glass', family: 'classic', style: 'solid'})

// 'solid' is the default style, you can leave that off
// icon({name: 'user'})

// 'classic' is the default family, you can leave that off
export const user = icon({name: 'user', style: 'solid'})
